// The one and only Firebase project this script may ever touch — checked
// three times over in firebaseAdmin.ts, the same pattern already proven in
// scripts/backfill-legs-won/src/constants.ts and scripts/showcase-seed.
export const EXPECTED_PROJECT_ID = 'chalkie-app';
