{
  "name": "showcase-seed",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Admin SDK script that creates/resets the isolated 'showcase-league' demo dataset in the chalkie-app Firebase project. Never run against any other project — see README.md before running anything.",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^22.0.0"
  }
}
